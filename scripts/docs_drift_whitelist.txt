# docs_drift_whitelist.txt
# Identifiers to skip in the Symbol-Existence check (Check 3).
# Format: one identifier per line, # for comments.
#
# Do NOT add project-specific code constants here (CC_ALPHA, HYBRID_CANDIDATES,
# DENSE_SCORE_THRESHOLD, etc.) — those must exist in source code.
# Add only: builtins, stdlib names, external-tool/framework acronyms, common
# English words that happen to be ALL_CAPS in docs, and external library functions.

# --- Python builtins / stdlib ---
DEBUG
ERROR
WARNING
INFO
NOTE
TODO
TRUE
FALSE
NONE

# --- Universal acronyms (already whitelisted in default seed) ---
API
CLI
CPU
GPU
HTTP
HTTPS
JSON
LOC
MCP
RAG
RAM
RRF
SOLL
IST
GUI
SDK
TOC
URL

# --- Doc status / section markers ---
DEPRECATED
REMOVED
ADDED
CHANGED
MOVED
FIXED
WIP

# --- ML / IR metric acronyms (appear in docs prose, not Python constants) ---
NDCG
IDCG
MRR
BLEU
ROUGE
BERT
BEIR
MTEB
TOIS

# --- Dataset / benchmark names ---
MARCO

# --- Framework / tool names (not Python constants in this codebase) ---
HNSW
RAGAS
YAML
GGUF
SETNX
MVCC

# --- Common English words that appear ALL_CAPS in docs context ---
MUST
WHILE
WORSE
SOLE
CRASH
LABEL
PATH
SKILL
STARTED
RESOLVED
README
NULL
SELECT
ALTER
TABLE
EXISTS
GROUP
CREATE
COLUMN
BEFORE
DEAD
CODE
DOCS

# --- Project-specific strings that are collection names / labels, not Python constants ---
# RAG_MCP is a collection name used in query files and docs prose
RAG_MCP

# --- External library functions referenced in docs but not in this project's source ---
# sentence-transformers method invoked via encode() parameter, not defined here
select_max_active_dims
